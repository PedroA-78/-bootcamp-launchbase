{% extends "layout.njk" %}

{% block content %}
    <main>
        <section class="descriptionContainer">
            <div class="decription">
                <h1>As melhores receitas</h1>
                <p>Aprenda a construir os melhores pratos com receitas criadas por profissionais do mundo inteiro.</p>
            </div>
            <div class="mascotContainer">
                <img src="/assets/chef.png">
            </div>
        </section>

        <section class="mostAccessed">
            <h2>Mais acessadas</h2>
            <div class="cards_MosAcc">

            {% for recipe in recipes %}

                <div class="card_MosAcc">
                    <div class="thumbContainer_MosAcc">
                        <img src="{{recipe.image}}">
                    </div>
                    <div class="cardInfo_MosAcc">
                        <h3 class="recipeTitle_MosAcc">{{recipe.title}}</h3>
                        <p class="recipeAuthor_MosAcc">por <span>{{recipe.author}}</span></p>
                    </div>
                </div>

            {% endfor %}

            </div>
        </section>
    </main>
{% endblock content %}